class Solution {
  public:
    // Function to find the height of a binary tree.
    int height(Node* node) {
    if (node == NULL) return -1; // If height should count nodes
    return max(height(node->left), height(node->right)) + 1;
    

        
