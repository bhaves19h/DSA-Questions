// Online C++ compiler to run C++ program online
#include <iostream>
#include <bits/stdc++.h>
#include <unordered_map>

using namespace std;
int main() {

    
    int arr[] = {1,2,1,2 ,3};
   unordered_map<int, int> freq; 
    
    for(int i : arr){
        freq[i]++;
    }

    for(auto &pair : freq){
        cout<<pair.first << "-> "
        << pair.second << endl;
    }
    
    
    return 0;
}
